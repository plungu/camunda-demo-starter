########################################
# Camunda Platform full development setup
########################################
version: '3.8'

volumes:
  certs:

networks:
  # Note there are two bridge networks. One for Zeebe and one for IAM. Operate, Tasklist, and Optimize use both
  camunda-cloud: { }
  camunda-platform: { }

services:
########################################
# Camunda Platform Service
########################################
  c8-client:
    container_name: c8-client
    build:
      context: ./camunda-8-spring-boot-client
    ports:
      - "9012:8080"
    volumes:
      - ./certs:/opt/certs
    environment:
      PORT: 8080
      PROFILES: prod,cors,cardinal
      CLUSTER_ID: be7a5c9e-915f-4944-b448-bbde59390f21
      CLIENT_ID: ze3-yz2AvK6ReorRxQ.cIBzKDD6CNXj5
      CLIENT_SECRET: iFrG86nyk_hG_rTQJJGXGkHm9fiFNcPimQwjCw2AjoW4t1lBPU0Ypbimzgrzo-oJ
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1000M
        reservations:
          memory: 1000M
    networks:
      - camunda-cloud
      - camunda-platform
#    depends_on:
#      - data-api
#      - camunda-postgres
#      - reactjs-demo
#      - smtp
#      - apacheds
#      - kafka
#      - rabbitmq-mgmt
